from django.shortcuts import render
from django.http import HttpResponse, JsonResponse
#TODO create views for the API endpoints that will be used to interact with the database tables

def remove(request):
    return HttpResponse('remove move this view and start coding!')